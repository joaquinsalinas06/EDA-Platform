---
kind: operation
title: Cut
order: 3
cppSteps:
  - step-1-node.cpp
  - step-2-structure.cpp
  - step-3-insert-union.cpp
  - step-4-cut-cascading.cpp
  - full-implementation.cpp
visualization:
  type: tree
  steps:
    - note: >-
        Estado inicial *(derivado del pseudocódigo)*: `r(1)` es raíz y
        `min(H)`, `p(8)` es su hijo, y `p` tiene tres hijos —
        `x(15)`, ya marcado por haber perdido un hijo antes, `y(20)` y
        `z(25)`. `grado(p) = 3`. Se va a llamar `Cut(H, x, p)`. **El
        detalle línea por línea de `removeFromList` y del puntero
        `p->child` está más abajo, en Algoritmo.**
      highlight: ["x"]
      nodes:
        - { id: r, value: 1, tag: r, parent: null }
        - { id: p, value: 8, tag: p, parent: r }
        - { id: x, value: 15, tag: x, parent: p, state: marked }
        - { id: y, value: 20, tag: y, parent: p }
        - { id: z, value: 25, tag: z, parent: p }
    - note: >-
        Paso 1a: se quita `x` de la lista de hijos de `p`. `x` se separa
        del enlace padre-hijo; a `p` sólo le quedan `y` y `z`.
      highlight: ["x", "p"]
      nodes:
        - { id: r, value: 1, tag: r, parent: null }
        - { id: p, value: 8, tag: p, parent: r, state: active }
        - { id: x, value: 15, tag: x, parent: null, state: active }
        - { id: y, value: 20, tag: y, parent: p }
        - { id: z, value: 25, tag: z, parent: p }
    - note: >-
        Paso 1b: `grado(p) ← grado(p) − 1`, de 3 a 2. Es una actualización
        de contador, separada de desenlazar a `x` — ninguna otra
        estructura se toca.
      highlight: ["p"]
      nodes:
        - { id: r, value: 1, tag: r, parent: null }
        - { id: p, value: 8, tag: p, parent: r, state: active }
        - { id: x, value: 15, tag: x, parent: null }
        - { id: y, value: 20, tag: y, parent: p }
        - { id: z, value: 25, tag: z, parent: p }
    - note: >-
        Paso 2a: se agrega `x` a la lista de raíces de `H` — empalme de
        punteros en O(1), igual que en
        [Insert](/structures/fibonacci-heap/operations/insert). `x`
        todavía conserva su marca vieja en este instante.
      highlight: ["x"]
      nodes:
        - { id: r, value: 1, tag: r, parent: null }
        - { id: p, value: 8, tag: p, parent: r }
        - { id: x, value: 15, tag: x, parent: null, state: marked }
        - { id: y, value: 20, tag: y, parent: p }
        - { id: z, value: 25, tag: z, parent: p }
    - note: >-
        Paso 2b: `padre(x) ← nulo` (ya lo estaba desde el paso 1a) y
        `marca(x) ← falso`. Como raíz, `x` no puede seguir marcado — la
        marca sólo tiene sentido en un nodo con padre.
      highlight: ["x"]
      nodes:
        - { id: r, value: 1, tag: r, parent: null }
        - { id: p, value: 8, tag: p, parent: r }
        - { id: x, value: 15, tag: x, parent: null, state: active }
        - { id: y, value: 20, tag: y, parent: p }
        - { id: z, value: 25, tag: z, parent: p }
    - note: >-
        Estado final: `x(15)` es raíz independiente y sin marca,
        `p(8)` quedó con `grado = 2` y sus hijos restantes `y`, `z`. Aquí
        `p` no estaba marcado, así que el corte termina sin propagarse. Si
        `p` ya hubiera estado marcado (por haber perdido un hijo antes),
        este mismo `Cut` sería el disparador de
        [Cascading-Cut](/structures/fibonacci-heap/operations/cascading-cut)
        sobre `p` — esa continuación no se repite aquí.
      highlight: []
      nodes:
        - { id: r, value: 1, tag: r, parent: null }
        - { id: p, value: 8, tag: p, parent: r }
        - { id: x, value: 15, tag: x, parent: null }
        - { id: y, value: 20, tag: y, parent: p }
        - { id: z, value: 25, tag: z, parent: p }
---
import Visualization from '../../../../src/components/Visualization.astro';

## Qué hace

Separa un nodo `x` de su padre `p` y lo agrega como raíz nueva en la lista
de raíces de `H`.

## Intuición

Es la operación de bajo nivel que hace posible que `Decrease-Key` sea
rápido: en vez de reordenar todo el árbol cuando una llave baja demasiado,
el nodo simplemente "escapa" hacia arriba, convirtiéndose en su propio
árbol de raíz. Como toda raíz, deja de estar marcado — la marca sólo tiene
sentido en un nodo que todavía tiene padre.

## Algoritmo

1. Quitar `x` de la lista de hijos de `p`; decrementar `grado(p)`.
2. Agregar `x` a la lista de raíces de `H`; `padre(x) ← nulo`;
   `marca(x) ← falso`.

### Quitar `x` de la lista de hijos, línea por línea

El paso 1 tampoco es atómico. En C++ son dos partes: primero reparar
`p->child` (el puntero de `p` a "algún hijo cualquiera" — si ese hijo
resulta ser justo `x`, hay que moverlo a otro antes de perderlo), y
después `removeFromList(x)`, la operación inversa de `spliceInto` de
[Insert](/structures/fibonacci-heap/operations/insert#el-empalme-de-punteros-línea-por-línea):
en vez de meter un nodo al ciclo, lo saca.

```cpp
if (p->child == x) {
    p->child = (x->right == x) ? nullptr : x->right;
}
removeFromList(x);
--p->degree;
```

```cpp
static void removeFromList(Node* x) {
    x->left->right = x->right;
    x->right->left = x->left;
    x->left = x->right = x;
}
```

<Visualization viz={{
  type: "tree",
  steps: [
    {
      note: "Antes: p tiene 3 hijos en su propio ciclo circular — x, y, z (p->child apunta a x, el 'algún hijo' arbitrario). Ciclo: x→y→z→x (right), x→z→y→x (left, el mismo ciclo al revés).",
      highlight: [],
      nodes: [
        { id: "p", value: 8, tag: "p", parent: null },
        { id: "x", value: 15, tag: "x", parent: "p", state: "marked" },
        { id: "y", value: 20, tag: "y", parent: "p" },
        { id: "z", value: 25, tag: "z", parent: "p" },
      ],
      links: [
        { from: "p", to: "x", kind: "pointer", label: "child" },
        { from: "x", to: "y", kind: "pointer", label: "R" },
        { from: "y", to: "z", kind: "pointer", label: "R" },
        { from: "z", to: "x", kind: "pointer", label: "R" },
        { from: "y", to: "x", kind: "pointer", label: "L" },
        { from: "z", to: "y", kind: "pointer", label: "L" },
        { from: "x", to: "z", kind: "pointer", label: "L" },
      ],
    },
    {
      note: "p->child == x, así que hay que moverlo ANTES de desenlazar: como x->right (y) no es el propio x, p->child pasa a ser y. (Si x fuera el único hijo, x->right sería x mismo — ver el caso aparte más abajo, donde p->child termina en nullptr.)",
      highlight: ["p", "x", "y"],
      nodes: [
        { id: "p", value: 8, tag: "p", parent: null },
        { id: "x", value: 15, tag: "x", parent: "p", state: "marked" },
        { id: "y", value: 20, tag: "y", parent: "p" },
        { id: "z", value: 25, tag: "z", parent: "p" },
      ],
      links: [
        { from: "p", to: "y", kind: "pointer", label: "child" },
        { from: "x", to: "y", kind: "pointer", label: "R" },
        { from: "y", to: "z", kind: "pointer", label: "R" },
        { from: "z", to: "x", kind: "pointer", label: "R" },
        { from: "y", to: "x", kind: "pointer", label: "L" },
        { from: "z", to: "y", kind: "pointer", label: "L" },
        { from: "x", to: "z", kind: "pointer", label: "L" },
      ],
    },
    {
      note: "removeFromList línea 1 — x->left->right = x->right;  x->left es z, x->right es y: z.right deja de apuntar a x y ahora apunta a y. z 'salta por encima' de x.",
      highlight: ["z", "y"],
      nodes: [
        { id: "p", value: 8, tag: "p", parent: null },
        { id: "x", value: 15, tag: "x", parent: "p", state: "marked" },
        { id: "y", value: 20, tag: "y", parent: "p" },
        { id: "z", value: 25, tag: "z", parent: "p" },
      ],
      links: [
        { from: "p", to: "y", kind: "pointer", label: "child" },
        { from: "x", to: "y", kind: "pointer", label: "R" },
        { from: "y", to: "z", kind: "pointer", label: "R" },
        { from: "z", to: "y", kind: "pointer", label: "R" },
        { from: "y", to: "x", kind: "pointer", label: "L" },
        { from: "z", to: "y", kind: "pointer", label: "L" },
        { from: "x", to: "z", kind: "pointer", label: "L" },
      ],
    },
    {
      note: "removeFromList línea 2 — x->right->left = x->left;  x->right es y, x->left es z: y.left deja de apuntar a x y ahora apunta a z. Con esto y y z ya se saltaron a x en las dos direcciones — sólo faltan los propios punteros de x.",
      highlight: ["y", "z"],
      nodes: [
        { id: "p", value: 8, tag: "p", parent: null },
        { id: "x", value: 15, tag: "x", parent: "p", state: "marked" },
        { id: "y", value: 20, tag: "y", parent: "p" },
        { id: "z", value: 25, tag: "z", parent: "p" },
      ],
      links: [
        { from: "p", to: "y", kind: "pointer", label: "child" },
        { from: "x", to: "y", kind: "pointer", label: "R" },
        { from: "y", to: "z", kind: "pointer", label: "R" },
        { from: "z", to: "y", kind: "pointer", label: "R" },
        { from: "y", to: "z", kind: "pointer", label: "L" },
        { from: "z", to: "y", kind: "pointer", label: "L" },
        { from: "x", to: "z", kind: "pointer", label: "L" },
      ],
    },
    {
      note: "removeFromList línea 3 — x->left = x->right = x;  x vuelve a auto-referenciarse (no se dibuja como flecha propia, mismo caso que un nodo recién creado en Insert). Sus dos flechas viejas hacia y/z (x→y R, x→z L) desaparecen del dibujo. y y z quedan en un ciclo de 2 — right y left vuelven a coincidir entre ellos, igual que el caso base de Insert.",
      highlight: ["x"],
      nodes: [
        { id: "p", value: 8, tag: "p", parent: null },
        { id: "x", value: 15, tag: "x", parent: null, state: "active" },
        { id: "y", value: 20, tag: "y", parent: "p" },
        { id: "z", value: 25, tag: "z", parent: "p" },
      ],
      links: [
        { from: "p", to: "y", kind: "pointer", label: "child" },
        { from: "y", to: "z", kind: "pointer", label: "R,L" },
        { from: "z", to: "y", kind: "pointer", label: "R,L" },
      ],
    },
    {
      note: "--p->degree: de 3 a 2. Sólo un contador — no reconecta nada, pero sin decrementarlo grado(p) mentiría sobre cuántos hijos tiene p de verdad.",
      highlight: ["p"],
      nodes: [
        { id: "p", value: 8, tag: "p", parent: null },
        { id: "x", value: 15, tag: "x", parent: null, state: "active" },
        { id: "y", value: 20, tag: "y", parent: "p" },
        { id: "z", value: 25, tag: "z", parent: "p" },
      ],
      links: [
        { from: "p", to: "y", kind: "pointer", label: "child" },
        { from: "y", to: "z", kind: "pointer", label: "R,L" },
        { from: "z", to: "y", kind: "pointer", label: "R,L" },
      ],
    },
    {
      note: "x->parent = nullptr; x->mark = false. x ya no depende de p para nada — ni como hijo (el ciclo de arriba ya no lo incluye) ni como marcado (las raíces nunca están marcadas).",
      highlight: ["x"],
      nodes: [
        { id: "p", value: 8, tag: "p", parent: null },
        { id: "x", value: 15, tag: "x", parent: null },
        { id: "y", value: 20, tag: "y", parent: "p" },
        { id: "z", value: 25, tag: "z", parent: "p" },
      ],
      links: [
        { from: "p", to: "y", kind: "pointer", label: "child" },
        { from: "y", to: "z", kind: "pointer", label: "R,L" },
        { from: "z", to: "y", kind: "pointer", label: "R,L" },
      ],
    },
    {
      note: "spliceInto(minNode, x) — el mismo empalme de Insert, línea por línea allá; acá sólo el resultado: x entra a la lista de raíces de H (no se dibuja aquí para no repetir el mismo diagrama dos veces).",
      highlight: ["x"],
      nodes: [
        { id: "p", value: 8, tag: "p", parent: null },
        { id: "x", value: 15, tag: "x", parent: null, state: "answer" },
        { id: "y", value: 20, tag: "y", parent: "p" },
        { id: "z", value: 25, tag: "z", parent: "p" },
      ],
      links: [
        { from: "p", to: "y", kind: "pointer", label: "child" },
        { from: "y", to: "z", kind: "pointer", label: "R,L" },
        { from: "z", to: "y", kind: "pointer", label: "R,L" },
      ],
    },
  ],
}} />

### Caso borde: `x` era el único hijo de `p`

Todo el rastro anterior asumió que `p` tenía más de un hijo, así que
`x->right` era otro nodo real (`y`). Si `x` es el único hijo, `x` está
solo en su propio ciclo — `x->right == x` — y eso cambia el primer paso:

<Visualization viz={{
  type: "tree",
  steps: [
    {
      note: "Antes: p tiene un solo hijo, x. Al estar solo en su ciclo, x->right == x y x->left == x (auto-referenciado, no se dibuja flecha — igual que un nodo recién creado). p->child apunta a x.",
      highlight: [],
      nodes: [
        { id: "p", value: 8, tag: "p", parent: null },
        { id: "x", value: 15, tag: "x", parent: "p" },
      ],
      links: [{ from: "p", to: "x", kind: "pointer", label: "child" }],
    },
    {
      note: "p->child == x, y esta vez x->right SÍ es x mismo — la condición del if se cumple con el otro resultado: p->child = nullptr. Se dibuja quitando la flecha 'child' por completo: p ya no apunta a ningún hijo, no a un nodo especial 'null'. (El trazo fino que sigue uniendo p con x es el borde de árbol de siempre, por el campo parent — todavía no se tocó; eso pasa recién más abajo.)",
      highlight: ["p"],
      nodes: [
        { id: "p", value: 8, tag: "p", parent: null },
        { id: "x", value: 15, tag: "x", parent: "p", state: "muted" },
      ],
      links: [],
    },
    {
      note: "removeFromList(x) corre igual, pero no cambia nada real: x->left->right = x->right sigue siendo x->right = x (x apuntándose a sí mismo con otro nombre); las 3 líneas son, en este caso, no-ops. Con un solo hijo no hay nadie más en el ciclo a quien reconectar.",
      highlight: ["x"],
      nodes: [
        { id: "p", value: 8, tag: "p", parent: null },
        { id: "x", value: 15, tag: "x", parent: null, state: "active" },
      ],
      links: [],
    },
    {
      note: "--p->degree: de 1 a 0. x->parent = nullptr, x->mark = false. p queda con grado 0 y child = nullptr — un nodo sin hijos, exactamente como cualquier hoja del montículo.",
      highlight: ["p", "x"],
      nodes: [
        { id: "p", value: 8, tag: "p", parent: null },
        { id: "x", value: 15, tag: "x", parent: null },
      ],
      links: [],
    },
  ],
}} />

`Cascading-Cut` (qué pasa cuando `p` ya estaba marcado y el corte se
propaga hacia arriba) tiene su propio diagrama en
[Cascading-Cut](/structures/fibonacci-heap/operations/cascading-cut).

## Pseudocódigo

```
Algoritmo 9: Cut(H, x, p)
Quitar x de la lista de hijos de p; grado(p) ← grado(p) − 1 ;
Agregar x a la lista de raíces de H; padre(x) ← nulo; marca(x) ← falso ;
```

## C++

Ver `step-4-cut-cascading.cpp` y `full-implementation.cpp` en el editor de
arriba.

## Complejidad temporal

Costo real $O(1)$: quitar `x` de una lista circular y agregarlo a otra son
ambas operaciones de reconexión de punteros, sin recorrer nada. El
profesor lo da directo, sin distinguir amortizado — el análisis amortizado
se hace sobre [Decrease-Key](/structures/fibonacci-heap/operations/decrease-key),
que es quien invoca `Cut` (a través de
[Cascading-Cut](/structures/fibonacci-heap/operations/cascading-cut)).

## Complejidad espacial

$O(1)$ adicional.

## Ejemplo

*(Derivado del pseudocódigo; no aparece en las diapositivas.)* Sea `p` un
nodo con hijos `{x, y, z}` y `grado(p) = 3`. `Cut(H, x, p)` quita `x` de
esa lista (quedan `{y, z}`, `grado(p) = 2`), agrega `x` a la lista de
raíces de `H`, y pone `padre(x) ← nulo`, `marca(x) ← falso`
(independientemente de si `x` estaba marcado antes).

## Casos límite

- **`x` es el único hijo de `p`**: `grado(p)` pasa a `0`, y como
  `x->right == x` (nadie más en el ciclo), la condición de
  `p->child = (x->right == x) ? nullptr : x->right` toma la rama del
  `nullptr` — el puntero de `p` a "algún hijo" queda en `nullptr` de
  verdad, no apuntando a ningún nodo. Ver el diagrama del caso borde más
  arriba.
- **`x` ya estaba marcado**: `Cut` lo desmarca igual — es la raíz que
  hace que la cuenta de nodos marcados $m(H)$ baje en el análisis de
  potencial de Decrease-Key.
- **`H` está vacío antes del corte**: no puede ocurrir en este mazo, porque
  `Cut` siempre se invoca sobre un nodo `x` con padre `p`, y si existe un
  nodo con padre, `H` ya tiene al menos ese árbol.
